apiVersion: v1
kind: Service
metadata:
  name: kmeans-frontend-service
spec:
  type: ClusterIP
  selector:
    app: kmeans-frontend
  ports:
    - protocol: TCP
      port: 5173
      targetPort: 5173